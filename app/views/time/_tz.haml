%tr{is: 'turbo-frame', id: "tz-#{tz.object_id}"}
  %td= tz.name
  %td= @time.in_time_zone(tz)
  %td
    Update via 
    = link_to 'Turbo', one_time_zone_path(tz.name)
    |
    = link_to 'Turbo Stream', one_time_zone_path(tz.name), data: {turbo_stream: true}
