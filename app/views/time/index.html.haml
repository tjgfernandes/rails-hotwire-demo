%h1 World Timezones

%p
  Local time: #{@time.in_time_zone('Europe/Lisbon')}

-# = turbo_stream_from 'timezones'

%p= link_to 'Update All', time_zones_path

%table
  %thead
    %tr
      %th Name
      %th Time
      %th Actions
  %tbody
    - @timezones.each do |tz|
      = render "tz", tz: tz


